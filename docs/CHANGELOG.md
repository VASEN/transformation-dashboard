# CHANGELOG — Transformation Dashboard

История правок велась в CLAUDE.md до 03.09.2026; с этого дня — здесь и в `git log`.
Добавлять строку сюда только за изменение, которое меняет поведение для пользователя
дашборда или формат сообщений; остальное — в сообщениях коммитов.

## История изменений (перенесено из CLAUDE.md 03.09.2026)

| Дата | Изменение |
|------|-----------|
| 2026-04-01 | Создан `process_report.py`, `README_report.md` |
| 2026-04-01 | Сортировка: приоритетные первыми, затем трансформационные; сортировка по дедлайну |
| 2026-04-01 | Исправлен дашборд: `status_text` → `current_status`; группа переименована в «Трансформационные» |
| 2026-04-01 | Признак приоритетности из `data.json` (`is_priority`), добавлен в `extract_data.py` |
| 2026-04-01 | Два отдельных файла: `telegram_priority_…` и `telegram_transform_…`; счётчик в заголовке; нумерация проектов |
| 2026-04-01 | Колонка приоритетности: `'Приоритетный проект'` == `'Да'` (было `'Приоритет'` с неверной логикой) |
| 2026-04-01 | Убраны Markdown-символы (`*`, `_`) — формат для ручного копирования в Telegram |
| 2026-04-01 | Поддержка URL без `#` (`transformation-back.rm.mosreg.ru/issues/N`) |
| 2026-04-02 | `extract_data.py`: добавлены поля `url`, `internal_hours`, `external_hours`, `total_units` в проекты |
| 2026-04-02 | `process_report.py`: добавлены `👤 owner_short`, `🔗 url`, `📉` блок высвобождения в `_render_group` |
| 2026-04-02 | Высвобождение: формат «X ч / Y шт.ед.», расчёт шт.ед. = часы / 1972 |
| 2026-04-02 | `index.html`: рефакторинг блока «Высвобождение» — `.gauges-layout`/`.gauges-grid`/`.gauge-detail-panel` заменены на `.vysv-grid` (grid 1fr 1fr); JS: `renderGauges`/`showGaugeDetail` → `renderVysvBlock`/`buildWideGaugeCard`/`buildCompactGaugeCard`/`buildDetailPanel`; komitet всегда wide span2; при выборе куратора — два wide-блока + деталь span2; `highlightGauge` делегирует `renderVysvBlock` |
| 2026-04-02 | `extract_data.py`: исправлена логика `urgency` — `get_urgency(dl, status)` возвращает `'ok'` для закрытых/выполненных задач; `CLOSED_STATUSES` расширен до всех закрытых статусов; `ACTIVE_STATUSES` убрано «Выполнено» |
| 2026-04-02 | `index.html`: `overdueProjects`/`todayProjects` в `renderFullProjTable` теперь исключают все закрытые статусы (не только «Закрыта») |
| 2026-04-03 | `index.html`: рефакторинг блока Высвобождение — фиксированная структура (detail panel span2 сверху + 4 compact-карточки 2×2); `renderVysvBlock` рендерит DOM один раз, затем только обновляет highlight и innerHTML панели; удалены `buildWideGaugeCard`/`buildCompactGaugeCard`/`buildDetailPanel`, добавлен `_vysvDetailHTML` |
| 2026-04-03 | `CLAUDE.md`: добавлен раздел «Блок Высвобождение %» с описанием layout, структуры рендера, поведения и CSS-классов |
| 2026-04-03 | `extract_data.py`: добавлены поля `vysv_internal_hours`, `vysv_external_hours` в объект куратора (из NaN-строк НМА); для «Комитет и РЦТ» суммируется по трём группам |
| 2026-04-03 | `index.html`: `_vysvDetailHTML` — панель детализации переработана: блок «Кадры» 2×3 (ккп/рцт объединены), блок «Высвобождение» 2 ячейки стопкой, акцент с цветом pct (=100 warn / >100 accent3 / <100 danger); `min-width:0` на всех ячейках |
| 2026-04-03 | `index.html`: таблица «Проекты — Готовность» — квартальная маркировка строк (`row-q1`–`row-q4`), бейджи Q1–Q4 в заголовке блока |
| 2026-04-03 | `CLAUDE.md`: обновлён раздел «Блок Высвобождение %» — новая структура `_vysvDetailHTML`, новые поля кураторов, описание квартальной маркировки |
| 2026-04-06 | `index.html`: блок «Высвобождение часов» (Детализация) расширен до 6 ячеек: план всего / план шт.ед. / внутреннее / внутр. шт.ед. / внешнее / внешн. шт.ед.; шт.ед. = hours/1972; часы — `--accent`, шт.ед. — `--accent2` |
| 2026-04-06 | `index.html`: `planUnits` вычисляется из `plan_hours/1972` вместо `d.plan_units` |
| 2026-04-06 | `index.html`: `currentStages` в `loadDetail()` сортируются по номеру этапа по возрастанию |
| 2026-04-06 | `index.html`: закрытые проекты в `loadDetail()` — блок «Текущий этап» показывает `✓ Реализован` + дату из `defense_at`; добавлены `isClosed`, `currentStageHTML` |
| 2026-04-06 | `index.html`: `populateDetailSelect` — закрытые проекты первыми с префиксом `✓`, сортировка по дедлайну внутри групп |
| 2026-04-06 | `index.html`: `renderProjTable` — закрытые проекты выводятся над активными с зелёной маркировкой `.row-closed`; вместо прогресс-бара показывается `✓ Завершён` |
| 2026-04-06 | `extract_data.py`: извлечение задач переведено с жёсткой 2-уровневой логики на итеративный обход (`while True`, L1–Ln); `stages`/`tasks` объединены в единый цикл → `all_tasks`; из `result` убраны ключи `stages`/`tasks` |
| 2026-04-06 | `extract_data.py`: добавлены поля `closed_at` и `defense_at` в объект проекта |
| 2026-04-06 | `deploy.sh`: создан скрипт деплоя; принимает `$1` (xlsx) и `$2` (md), пробрасывает в python-скрипты |
| 2026-04-07 | `index.html`: `CLOSED_STATUSES` и `byDeadline` вынесены в глобальные константы; удалены 5 локальных копий (`CLOSED`, `_closed`, `notActive`, `CLOSED_STATUSES`, `byDeadline`) из `renderProjTable`, `renderFullProjTable`, `computeTaskKPIs`, `populateDetailSelect`, `filterTasksByStat` |
| 2026-04-07 | `index.html`: `currentStageHTML` упрощён — убран лишний IIFE с массивом `lines`, заменён прямым тернарным выражением |
| 2026-04-07 | `index.html`: `populateDetailSelect` — двойной вызов `CLOSED.has()` устранён: `closed`/`active` итерируются отдельно |
| 2026-04-08 | `index.html`: хотфикс — `notActive` (необъявленная переменная после рефакторинга) заменена на глобальный `CLOSED_STATUSES` в `computeTaskKPIs` |
| 2026-04-08 | `index.html`: маркер `★` (`var(--warn)`) для приоритетных проектов (`is_priority: true`) в `renderProjTable`, `renderFullProjTable`, `populateDetailSelect` |
| 2026-04-08 | Зависимости: на macOS Python 3.13 может потребоваться `pip3 install --upgrade numpy pandas openpyxl` |
| 2026-04-08 | `index.html`: `PRIORITY_STAR` вынесен в глобальную константу; инлайн-стиль заменён CSS-классом `.priority-marker` |
| 2026-04-08 | `process_report.py`: парсер MD расширен — поддержка нумерованных проектов (`N. 🔵 Название`); регулярка `^([🔵🔴])` → `^(?:\d+\.\s*)?([🔵🔴])`; устранён баг: URL следующего проекта ошибочно присваивался предыдущему |
| 2026-04-08 | `process_report.py`: добавлен флаг `--prev PREV_REPORT` для diff-режима; новые функции `strip_dates`, `normalize_line`, `compute_new_lines`, `find_prev_project`, `build_prev_index`, `render_block_with_diff`; строки блоков "Выполнено"/"В работе", отсутствующие в предыдущем отчёте, маркируются `🆕`; новые проекты получают суффикс `🆕 новый проект`; в шапку сообщений добавляется строка `🔄 Сравнение с отчётом от ДД.ММ.ГГГГ` |
| 2026-04-08 | `deploy.sh`: добавлен `$3` — предыдущий md-отчёт для сравнения; при наличии пробрасывает `--prev` в `process_report.py` |
| 2026-04-09 | `process_report.py`: фикс дублирования заголовка «В работе» — парсер теперь стрипает `📍 В работе:` наравне с `📍 Текущие этапы:` (регулярка расширена: `(?:Текущие\s*этапы\|В работе)`) |
| 2026-04-09 | `CLAUDE.md`: добавлен раздел «Diff-режим (--prev)» с описанием всех вспомогательных функций; раздел «Парсер 📍-строк»; `--prev` добавлен в примеры запуска |
| 2026-04-09 | `index.html`: вкладка «Задачи» — тема задачи обёрнута в `<a href="https://transformation.rm.mosreg.ru/#/issues/{id}">` (target=_blank); при отсутствии `id` — plain text; добавлен CSS-класс `.task-link` (dashed underline, cyan hover) |
| 2026-05-28 | `overdue_report.py`: новый скрипт — отчёт по просроченным задачам на текущую дату. Читает `data.json` (`urgency == 'overdue'` или дедлайн < today и статус не в `CLOSED_STATUSES`), группирует по проекту, сортирует по max days overdue. Для каждого проекта: ответственный (`owner_short`), URL; для задачи: исполнитель (`executor_short`), дедлайн, дней просрочки, статус, ссылка. Выход: `overdue_tasks_DD_MM_YYYY.txt`. Флаги: `--data`, `--output` |
| 2026-05-28 | `deploy.sh`: после `process_report.py` добавлен вызов `python3 overdue_report.py` |
| 2026-05-29 | `overdue_report.py`: в отчёт включены задачи со сроком сегодня. Фильтр изменён с `dl < today` на `dl ≤ today`; задачи внутри проекта разделены на подсекции `⏰ Просроченные задачи` и `📅 Срок сегодня`; обновлены агрегаты группы (`total_overdue` / `total_today` / `total`), шапка отчёта и итоговый лог |
| 2026-06-15 | `extract_data.py`: поддержка нового формата файла высвобождения (колонка `Ответственный`, итоги кураторов в строках-заголовках). Добавлены `SHTATKA_FILE`/`VYSV_FILE` как опциональные CLI-аргументы (`$2`/`$3`); хелперы `cell_any` (совместимость имён колонок: `План, шт. ед.`/`Внешнее высвобождение`), `curator_key` (матчинг кураторов по фамилии), `canon_name` + `CURATOR_NAME_FIX` (исправление `Кудряшев → Кудряшов`). Автоопределение формата по наличию колонки `Ответственный`; старый формат (NaN-строки + `CURATOR_ORDER`) сохранён как fallback |
| 2026-06-15 | Данные: добавлен 4-й куратор `Кудряшов Е.С.`; обновлены штатка и высвобождение на 15.06.2026 (план-20%: Кренёва 7.2, Родевальд 11.6, Гуляев 4.6, Кудряшов 3.0, Комитет 26.4). Пересчитан `data.json`: % высвобождения 127/134/96/92, итог 121% |
| 2026-06-15 | `CLAUDE.md`: обновлён раздел «Кураторы и высвобождение» (4 куратора, формат файла, матчинг по фамилии, исправление написания `Кудряшов`); добавлены примеры запуска с тремя файлами |
| 2026-06-16 | Документация: `CLAUDE.md` — добавлен раздел «Архив (`archive/`)», заметка про датированные исходники и `data.json.bak`, раздел «Обновление данных» приведён к актуальному `deploy.sh` (аргументы `$4`/`$5`, автоопределение штатки/высвобождения, полный пайплайн с `overdue_report.py`). `README_report.md` — два telegram-файла (priority/transform) вместо одного, добавлен флаг `--prev`, ссылка на подробности в `CLAUDE.md` |
| 2026-06-16 | Порядок в папке: старые датированные файлы перенесены в `archive/` (`issues/`, `reports/`, `telegram/`, `overdue/`, `source/`) — оставлен набор за последнюю неделю (с 09.06). `ОТЧЕТ_*.md` (кроме 10.06) перенесены через `git mv`; `data.json.bak` снят с отслеживания и добавлен в `.gitignore` вместе с `.DS_Store`. Архивные `*.xlsx`/`*.txt` остаются вне git (глобальные правила `.gitignore`); deploy-глобы и `git add .` их не цепляют |
| 2026-06-16 | `deploy.sh`: теперь передаёт штатку и высвобождение в `extract_data.py` (раньше — только Redmine-файл, из-за чего бралась старая мартовская штатка и кураторы откатывались к 3). Добавлены аргументы `$4` (штатка) / `$5` (высвобождение) с автовыбором самого свежего файла по `ls -t` (`ШТАТКА_ДБ*.xlsx` / `*Данные по высвобождению*.xlsx`, `shopt -s nullglob`), fallback — каноническое имя; добавлен лог выбранных файлов |
| 2026-06-16 | **Фаза 1 «Надёжность + Качество»** (ветка `phase1-reliability-quality`). Спека `docs/specs/2026-06-16-phase1-reliability-quality-design.md`, план `docs/plans/2026-06-16-phase1-reliability-quality.md`. 11 коммитов; приёмка: 22 теста зелёные, контрольный прогон даёт те же цифры (36/613/5, просрочено 17, высвобождение 122%) + ключ `config` |
| 2026-06-16 | `config.py` (новый): единый источник констант (`YEAR`, `REDMINE_BASE`, `HOURS_PER_UNIT=1972`, `CURATOR_ORDER`, `CLOSED_STATUSES`, `COLUMN_SYNONYMS`) + резолвер колонок (`normalize_col`/`resolve_column`/`require_column`) + `public_config()`. [R3] |
| 2026-06-16 | `extract_data.py`: константы импортируются из `config`; `extract()` параметризован (`redmine_file`/`shtatka_file`/`vysv_file`/`output_file`), разбор `sys.argv` перенесён в `__main__` (импорт без побочных эффектов — нужно для тестов); в `data.json` добавлен ключ `config`; запись через `write_json_atomic` (tmp → парс-проверка → `os.replace`). [R3/R1] |
| 2026-06-16 | `extract_data.py`: `validate_source_columns` — проверка обязательных колонок Redmine/штатки/высвобождения с понятной ошибкой вместо `KeyError`. [R2] |
| 2026-06-16 | `extract_data.py`: `validate_result` — стоп при пустом результате или просадке проектов/задач > 50% относительно прошлого `data.json`; битая выгрузка больше не затирает рабочий файл (атомарная запись). [R1] |
| 2026-06-16 | `process_report.py` / `overdue_report.py`: `HOURS_PER_UNIT` / `REDMINE_BASE` / `CLOSED_STATUSES` берутся из `config` (убраны локальные дубли). [R3] |
| 2026-06-16 | `index.html`: глобал `CONFIG` читается из `data.config` (с фолбэком на дефолты); убран хардкод года `2026` (5 мест) и `1972` (4 места), URL Redmine — из `CONFIG.redmineBase`. [R3] |
| 2026-06-16 | `index.html`: добавлен `escapeHTML()` и применён к строкам из `data.json` (имена проектов/исполнителей, темы, статусы, `current_status`, цель, показатели) — защита от поломки разметки. [R5] |
| 2026-06-16 | `deploy.sh`: `git add .` → `git add data.json`; guard'ы — проверка непустого/валидного JSON, пропуск коммита при отсутствии изменений (`git diff --cached --quiet`), пуш только в существующие remotes. [R4] |
| 2026-06-16 | `tests/` (новый): pytest — юниты резолвера колонок и хелперов (`get_urgency`, `short_name`, `curator_key`, `canon_name`, `clean_pct`, `clean_date`, `parse_deadline`, `plural_*`) + интеграционный smoke на Excel-фикстуре (`fixtures.py`): валидный вход → форма `data.json` + ключ `config`; битый/пустой → `data.json` не затирается. Итого 22 теста. [Q1] |
| 2026-06-16 | `pyproject.toml` (новый): конфиг `ruff` + `pytest`; type hints на ключевых функциях `extract_data.py`. Прогон `ruff` отложен (нет доступа к сети в текущей среде) — конфиг готов, синтаксис проверяется через `py_compile`. [Q2] |
| 2026-06-16 | **Фаза 2 «Дизайн»** (ветка `phase2-design`). Спека `docs/specs/2026-06-16-phase2-ui-hardening-design.md`. Эстетика сохраняется; под-фазы D4→D1→D2→D3. Решения: адаптив на телефон/десктоп/крупные; мобильные карточки задач + нижняя навигация; ES-модули по функциям |
| 2026-06-16 | **D4 — модуляризация `index.html`** (план `docs/plans/2026-06-16-phase2-d4-modularization.md`). CSS вынесен в `css/styles.css`; JS разбит на ES-модули `js/` по функциям (`config`/`helpers`/`state`/`render/*`/`filters`/`nav`/`main`); `index.html` → `<script type="module" src="js/main.js">`. Логика не менялась — дашборд идентичен (36/613/5, 122%). Стратегия «строим рядом, переключаем последним»; верификация: `node --check` + резолвинг графа `import('./js/main.js')` + статический анализ полноты импортов (0 проблем) + ручная визуальная сверка |
| 2026-06-16 | **D1 — адаптив + доступность** (план `docs/plans/2026-06-16-phase2-d1-responsive-a11y.md`). Брейкпоинты: телефон ≤640 (одна колонка, нижняя навигация `.bottom-nav`, таблицы→карточки через CSS-reflow + `data-label`), десктоп 641–1599 без изменений, крупные ≥1600 (`max-width` + крупнее типографика). Эстетика не менялась |
| 2026-06-16 | `index.html`/`css/styles.css`/`js/{nav,main}.js`: D1 — нижняя навигация ≤640 зеркалит 3 вкладки (`showTab` синхронит `.tab`/`.bottom-tab` + `aria-current`); `goToDetail` через `showTab('detail')` (сохранён `loadDetail`); карточки задач/проектов на телефоне |
| 2026-06-16 | a11y [D1]: роли вкладок (`tablist/tab/tabpanel`, `aria-selected`, клавиатура ←/→/Home/End), семантический `<main>`, `aria-label` на фильтрах, `:focus-visible`, контраст `--text-dim`→0.72, `aria-live` на KPI/счётчиках, `role=img`+`aria-label` на SVG-гейджах. Аудит `web-design-guidelines` — блокеров нет; доводка: tabpanel без лишнего `tabindex`, `aria-current` на нижней нав., `aria-hidden` спарклайнов, `prefers-reduced-motion` |
| 2026-06-16 | Находка: экран `screen-projects` недостижим в UI (только 3 вкладки exec/detail/tasks, нет `showTab('projects')`) — оставлен как есть по решению владельца; нижняя навигация зеркалит реальные 3 вкладки. Вопрос «вернуть Проекты 4-й вкладкой» отложен |
| 2026-06-16 | **D3 — дизайн-система в Claude Design**: собрана библиотека `design-system/` (14 самодостаточных превью с `@dsCard` на реальных токенах — KPI/stat-pill, гейдж, прогресс-бар, статус/дедлайн/project/quarter бейджи, приоритет-звезда, вкладки, нижняя навигация, filter-select, карточка задачи, токены) и синхронизирована на claude.ai/design через `DesignSync` (проект «Transformation Dashboard — Design System», projectId `6659eb05-…`). Группы: Карточки/Высвобождение/Таблицы/Бейджи/Навигация/Формы/Токены |
| 2026-06-16 | Чистка: удалён недостижимый экран «Проекты» (`screen-projects`) — он дублировал таблицу «Проекты — Готовность» на Обзоре, а детализация по проекту есть на вкладке «Детализация». Удалены блок разметки, `js/render/projects.js` и 4 ссылки в `main.js`; `renderProjTable` (Обзор) и `goToDetail` сохранены. Теперь 3 экрана: exec/detail/tasks |
| 2026-07-22 | `process_report.py`: реализованные проекты больше не теряются в сводке — секция `✅ РЕАЛИЗОВАНО В 2026 (N)` сразу после шапки (берётся из `data.json`, только текущий год `config.YEAR`, дата `closed_at`→`defense_at`, ссылка на Redmine) + строка портфеля `📈 Портфель: N · ✅ реализовано M · 🔄 в работе K` в шапке. Новые функции: `completed_date`, `collect_completed`, `project_url`, `_portfolio_line`, `_render_completed`; `build_priority_message`/`build_transform_message` принимают `completed_projs`/`in_work_count`. Решения владельца: только текущий год (прошлые не показываем), строка со ссылкой, секция в начале сообщения |
| 2026-07-22 | `process_report.py`: примечания к проекту (`ℹ️ Дополнительно`) — свободный абзац в конце блока `✅`/`📍` больше не приклеивается хвостом к списку этапов. `split_note()` + `NOTE_MARKER_RE` (явные маркеры `ℹ️`/`Доп. информация:`/`Примечание:` …) + эвристика связного текста (`NOTE_MIN_FREE_TEXT_LEN=100`, `LIST_ITEM_RE` не считает дату `11.06.2026` нумерацией). Примечание попадает в `current_status` (виден на дашборде) и в Telegram отдельным блоком; при `--prev` помечается `🆕`, если изменилось. Проверка на 17 отчётах: 6 срабатываний, все настоящие, ложных нет |
| 2026-07-22 | `tests/test_report_completed.py` (новый): 15 тестов на `completed_date`/`collect_completed`/`project_url`/`split_note`/`build_current_status` (в т.ч. краевые: подпункт этапа и буллет-список не принимаются за примечание, закрытый проект без даты не попадает в секцию). Итого 37 тестов |
| 2026-07-22 | По итогам ревью (вердикт NO-GO → исправлено): **блокер** — проект, закрытый на отчётной неделе, попадал одновременно в «Реализовано» и в список «в работе», а счётчик группы расходился с `🔄 в работе N` (воспроизводилось на `ОТЧЕТ_01.07.md` + `data.json` от 01.07: «ИИ-Ассистент ЕАСУЗ» дважды, группа (19) против «в работе 18»). Добавлен `is_closed_report_project`, закрытые проекты отчёта исключаются из групп в `main()` и логируются. Минорные: строка портфеля больше не зависит от непустого списка реализованных (выводится при наличии `data.json`, ноль показывается как `реализовано 0`, убран фолбэк `or len(...)`); закрытые без дат логируются предупреждением; `_with_note` убирает ложный `🆕` при переходе абзаца примечание↔тело блока; абзац из одного маркера без текста примечанием не считается |
| 2026-07-22 | По итогам второго круга ревью (вердикт GO, остаточные замечания закрыты): `is_closed_report_project` → `in_completed_section(proj, completed)` — из группы «в работе» убираются только проекты, реально попавшие в секцию «Реализовано» (закрытый в прошлом году или без даты больше не исчезает из сообщения; триггер — январский отчёт с проектом, закрытым в декабре). `note_changed` считается через `compute_new_lines` против тела **и** примечания прошлой недели, а `normalize_line` дополнительно срезает маркер примечания — ложный `🆕` при переезде абзаца тело↔примечание убран в обе стороны |
| 2026-07-22 | `extract_data.py`: у «Цифровое нормирование 2.0» не показывались трудозатраты — в файле высвобождения проект называется «Цифровое нормирование», а матчинг шёл по точному имени (`vysv_proj.get(p['name'].strip())`). Добавлен `project_key()` (lowercase, `ё→е`, отброшен хвост-версия `2.0`, только буквы/цифры) — сопоставление по нормализованному ключу; хвост `223-ФЗ` версией не считается. В лог добавлены диагностики: `~ сопоставлены по нормализованному имени` и `⚠️ активные проекты без строки в файле высвобождения`. Контрольный прогон: заполнились только поля этого проекта (внутр. 611 ч, внеш. 47 517 ч), остальные проекты и итоги кураторов (122/127/138/96/92) не изменились; коллизий ключей среди 36 проектов нет. +5 тестов в `tests/test_helpers.py`, итого 51 |
| 2026-07-22 | `extract_data.py`: **строки одного проекта суммируются** — проект, который ведут несколько кураторов, разнесён в файле высвобождения по долям (строка в блоке своего куратора), раньше побеждала последняя строка и цифры занижались. Добавлены `VYSV_SUM_FIELDS` + `sum_opt`, аккумулирование вместо перезаписи, `url` — первый непустой, в лог добавлен блок `Σ проекты, разнесённые по долям кураторов`. По разъяснению владельца (22.07): доли относятся к конкретным руководителям, верная величина по проекту — их сумма. Итог прогона: «Цифровой двойник сотрудника (ИИ)» 919 → 8088 ч внутр. + 2329 ч внеш., «Анализ проектов ГЧП» 912 → 1824 ч; сумма `total_units` по проектам сошлась с итогом «Комитет и РЦТ» — 32.30 (было 27.89), проценты кураторов не изменились. +5 тестов (`sum_opt`, суммирование долей на Excel-фикстуре с `split_shares`), итого 54 |
| 2026-07-22 | По итогам третьего круга ревью (вердикт GO): закрыта латентная дыра — если в Redmine окажутся сразу «X» и «X 2.0», их `project_key` совпадёт и оба проекта получили бы одни и те же часы. Теперь неоднозначные ключи детектируются (`Counter` по проектам), оверлей к ним не применяется, в лог идёт `⚠️ неоднозначные названия проектов`. Ревьюер подтвердил сходимость до единицы: Σ`internal_hours` 63 686 = `vysv_internal_hours` куратора, Σ`external_hours` 595 372 = `vysv_external_hours`, Σ`total_units` 32.295 ≈ 32.30; блоки `curators`/`summary`/`all_tasks`/`config` идентичны прежним. +2 теста (неоднозначные версии, итоги кураторов при долях), итого 56 |
| 2026-07-22 | `tests/test_report_message.py` (новый): 9 интеграционных тестов рендера — скрипт запускается процессом во временном каталоге (сборка списков в `main()` ранее не покрывалась): нет дубля закрытого проекта и согласованность счётчиков, формат и позиция секции «Реализовано», отдельный блок `ℹ️ Дополнительно`, `🆕` на изменившемся примечании и его отсутствие на неизменном, скрытие секции при нуле реализованных, попадание проекта без отчёта в `⚠️ ИНФОРМАЦИЯ НЕ ПРЕДСТАВЛЕНА`. Итого 44 теста |
| 2026-07-24 | `deploy.sh`: падение пуша в один remote больше не блокирует остальные — `git push` каждого remote обёрнут в `if !` (иначе `set -e` убивал скрипт на первом же сбое), неудачные remotes копятся в `push_failed`, в конце — `exit 1` со списком. Причина: 24.07 пуш в `upstream` (VASEN) стал падать с 403 (доступ у `johnmiller03535` отсутствует), и из-за порядка `upstream → origin` коммиты не доходили и до `origin`. Доступ к `upstream` восстанавливает владелец |
| 2026-07-22 | Локальный скилл `pipeline-preview` (`.claude/skills/pipeline-preview/`): `preview.sh` + `compare_data.py` — прогон текущей и базовой (`--base`, по умолчанию `HEAD`) версий скриптов на одних входах в песочнице и diff результата: диагностики пайплайна, изменения полей `data.json`/кураторов, построчный diff telegram-сообщений и проверка инварианта «сумма шт.ед. по проектам ↔ итог Комитет и РЦТ». Извлечён из повторяющегося за сессию ручного действия (копирование скриптов и данных в скретч-каталог). Проверен на ревизии до правок: показывает `base: 27.89 ↔ 32.30 ⚠️ РАСХОЖДЕНИЕ` против `work: 32.30 ↔ 32.30 ✅` |
| 2026-08-13 | `upcoming_report.py` (новый): справка по задачам с приближающимся сроком за произвольное окно дат — отвечает на вопрос «что уйдёт в просрочку», которого не покрывал `overdue_report.py` (тот смотрит только назад). CLI: `--from/--to/--days`, `--format txt|md|both`, `--include-overdue`, `--stdout`, `--outdir/--output/--data`. Два формата одного содержания: txt как `overdue_tasks_*.txt` и md как `СПРАВКА_*.md` (разбивка по датам, детализация таблицами, краткая сводка, блок «Обратить внимание»). Скрипт только читает `data.json`. Обёртка — локальный скилл `.claude/skills/upcoming-report/`. Тесты `tests/test_upcoming_report.py` (29), итого по проекту 85 |
| 2026-08-13 | По ходу разработки закрыты два дефекта: (1) при окне, начинающемся в будущем, счётчик «ранее просроченных» считал от начала окна и завышал просрочку — разведены `before_window` (срок < начала окна) и `overdue_now` (просрочено на сегодня), формулировку выбирает `before_window_line`; (2) перевёрнутое окно и нечитаемый `--data` падали трейсбеком — теперь `ap.error` с понятным текстом и без записи файлов |
| 2026-08-13 | `.gitignore`: добавлен `СПРАВКА_*.md` — справки (`upcoming_report.py` и ручные) в GitHub не уходят. `.txt`-версии уже покрывал общий `*.txt`; отслеживаемых справок в индексе не было, снимать ничего не потребовалось |
| 2026-08-17 | `pokazateli-ct/` (новая папка): страница показателей цифровой трансформации — рейтинг ЦИО по кварталам (8 блоков, 35 подпоказателей, 100 баллов). По каждому подпоказателю видно порог, факт и цену в баллах; отдельно вынесены «ждём данных» и «теряем баллы». Значения 3 квартала собраны опросом владельца 17.08.2026: расчёт 77 баллов, потолок 92 после выгрузки МСЭД (Q1 = 70, Q2 = 85). **Расчёт и факты здесь не дублируются** — методика (`methodology.json`), факты по кварталам (`facts.json`), правила подсчёта (`score.py`) и живые числа (`live.py`) живут в `~/Projects/LIFE/rating/`, `build.py` их импортирует; второй источник значений завёл бы расхождение в учёте. Просрочки подставляются живым числом из свежей выгрузки Redmine (на 17.08 — ноль, впервые за три квартала порог ≤ 20 взят). Стиль — тёмная тема проекта (`design-system/tokens.html`) |
| 2026-08-18 | `pokazateli-ct/` перерисована на **общую дизайн-систему панели отчётов** (`~/Projects/LIFE/otchety/dsh.py` — порт возврата Claude Design, круг 1). Тот же набор компонентов рисует вкладку «Показатели ЦТ» в панели и страницу статуса роликов в `presentations`: раньше один отчёт выглядел по-разному в зависимости от того, откуда открыли. Разметку компонентов править только в `dsh.py`. Правило чтения: форма знака отвечает, откуда значение (факт · срок · пропуск · расчёт), цвет — хорошо это или плохо; нет данных = прочерк с причиной, не ноль. Содержание не менялось (8 блоков, 35 подпоказателей, расчёт в `LIFE/rating/`); `styles.css` собирается из возврата дизайна и в git не идёт (`.gitignore`) |
| 2026-08-21 | `pokazateli-ct/` **удалена: переехала в `~/Projects/DIGITAL_KPI`** вместе с расчётом, который до сих пор жил в `LIFE/rating/`. Показатели ЦТ хранились в двух местах — подача здесь, методика и факты в LIFE, — и решение владельца собрать их в одном привело к отдельному проекту. Здесь их больше нет и заводить заново нельзя: второй источник значений разойдётся с учётом через квартал. Выгрузки `issues_*.xlsx` **остаются здесь** — они питают просрочки, дашборд и панель отчётов, а не только ЦТ; новый проект читает их отсюда через `LIFE/bin/redmine_watch.py`. Строки журнала за 17–18.08 оставлены как история |
| 2026-08-24 | `watch_pipeline.sh` + `automation/` (новые): автопрогон пайплайна по появлению свежей выгрузки. LaunchAgent `com.vz.transformation-pipeline` (WatchPaths на корень) → переименование выгрузки в текущую дату (МСК) → `deploy.sh` без md-отчёта → `upcoming_report.py` до ближайшей пятницы. Решения владельца 24.08: telegram-сводку автопрогон не трогает (собралась бы по прошлонедельному отчёту), пуш включён (обновляет дашборд на Amvera), окно справки — до пятницы, файл дня при повторном забросе перезаписывается молча. От зацикливания — замок, отпечаток обработанной выгрузки и «только сегодняшняя»; исход «пуш не прошёл в один remote» отделён от падения пайплайна (у `upstream` слетал доступ — 403). Логи `logs/` в git не идут |
| 2026-08-24 | `watch_pipeline.sh`: отчёт о прогоне в Telegram (канал `LIFE/bin/tg.py`, тот же бот, что у `redmine_watch`). Успех — файл, цифры портфеля из `data.json`, сводка справки, статус пуша; сбой — `fail()` перед каждым аварийным выходом: шаг, код, хвост лога текущего прогона, команда повтора `--force`. `log_tail` режет лог по маркеру `─── новая выгрузка`, иначе в сообщение затекала история прошлых прогонов. Telegram недоступен → предупреждение в лог, прогон не падает. Проверено на заглушках по всем четырём исходам deploy |
| 2026-08-24 | По итогам независимого ревью (вердикт NO-GO → исправлено). **Блокеры:** (1) отпечаток писался только на успешном пути — упавший `deploy.sh` повторялся бесконечно (launchd будит агент ~6 раз в минуту), теперь пишется до работы, повтор только по `--force`; (2) `mv -f` шёл до всякой проверки и посторонний `issues*.xlsx` затирал сегодняшний снимок Redmine безвозвратно — добавлена проверка колонок до переименования; (3) старый снимок из `archive/` (после `cp` mtime свежий) публиковался как актуальные данные — сверяется дата в имени; (4) файл, отбракованный по дате, отбрасывался вообще без следа — теперь лог + сообщение с подсказкой `--force`, отпечаток в `logs/.skipped` от повторов. **Минорное:** отпечаток снимался до ожидания стабилизации размера (лишний прогон); замок снимался по возрасту без проверки живости процесса и `trap` удалял чужой — теперь PID + `kill -0`; подрезка лога вынесена под замок; `osascript` получает текст аргументом, а не склейкой строк. **`deploy.sh`:** `git commit -- data.json` (без pathspec уезжало всё из индекса) и остановка, если HEAD не на `main`. `tests/test_watch_pipeline.py` (новый): 10 сценариев автопрогона в песочнице, итого по проекту 95 |
| 2026-08-24 | Второй круг ревью (вердикт GO; четыре блокера подтверждены закрытыми исполнением). Закрыты три дефекта, внесённые самими исправлениями: (1) цикл захвата замка `while true` мог крутиться вечно при недоступном `logs/` — метка агента занималась навсегда, автопрогон вставал молча; теперь три попытки и выход с сообщением; (2) провал проверки по причине среды (рассинхрон numpy/pandas — прецедент 08.04) помечал настоящую выгрузку обработанной и ставил неверный диагноз «файл чужой»; теперь код возврата 2 (чужой) и 3 (проверка не выполнилась) разведены, во втором случае отпечаток не пишется и выгрузка подхватывается после починки среды; (3) прогон, убитый сигналом между записью отпечатка и концом работы, выглядел обработанным — выгрузка пропадала молча при вчерашнем `data.json`; в `.processed` добавлен статус `started`/`done`/`failed` + pid, оборванный прогон повторяется со строкой «↻ прошлый прогон был оборван». Ниты: подсказка `--force` в сообщении о чужом файле, подрезка лога больше не выполняется в `--dry-run`, в Telegram не уходит список колонок постороннего файла. +4 теста (все краснеют на прошлой ревизии), итого 99 |
| 2026-08-24 | `docs/manuals/` (новая папка): памятка по автопрогону для владельца — `MANUAL_автопрогон.md` и `how-it-works.html`. Разбор шести сообщений Telegram по схеме «что пришло → что значит → что делать»; тексты сообщений взяты из `watch_pipeline.sh` как есть. Отдельным блоком — единственный случай, когда Telegram молчит принципиально: недоступный на запись `logs/` не даёт ни писать лог, ни отправить сообщение, остаётся только всплывашка macOS «Дашборд: автопрогон встал». Скилл `manual` применён с двумя отступлениями: рендер здесь, а не в Cowork (документ на страницу, экранов приложения нет), ревью-гейт не запускался — тексты выдернуты грепом по коду, а не по памяти |
| 2026-08-26 | `report_inbox.py` + `report_reminder.py` (новые): сбор еженедельных отчётов через Telegram. Владелец пересылает боту блоки, тот раскладывает их по проектам (ключ — ссылка на Redmine), считает сдавших от активных проектов `data.json`, а когда сдали все — сам собирает `ОТЧЕТ_ДД.ММ.md`, зовёт `process_report.py` с diff против прошлой недели и отдаёт обе сводки. Парсер не писался: формат присылаемого совпал с тем, что уже читает `parse_report` (проверено на реальном сообщении владельца). Напоминание о несдавших — по средам 12:00 МСК; агент будят в 04:00 и 05:00 по времени машины, а нужный час скрипт определяет сам по МСК (Нью-Йорк переходит на зимнее время, Москва нет). Со стороны бота: автораспознавание отчёта без команды, достройка блока без строки 🔵 по ссылке, кнопки под ответами, heartbeat + `/ping` + доклад о простое после сна ноутбука. Цикл закрывается после полной сборки — иначе следующая неделя стартовала бы с «все сдали». +12 тестов, итого 111. Первый живой прогон 26.08: 17 отчётов приняты, сводки собраны автоматически, неделя закрыта |
| 2026-08-27 | `automation/install.sh`: `--status` показывал «⛔️ не загружен» для реально работающего агента. Причина — `set -o pipefail` плюс `launchctl list | grep -q`: `grep -q` закрывает пайп на первом совпадении, `launchctl` получает SIGPIPE, и весь конвейер считается провальным. Коварство в том, что зависит от позиции метки в выводе: для `transformation-pipeline` совпадение во второй строке и проверка «работала», для `report-reminder` — в первой, и она стабильно врала. Переведено на `launchctl print gui/UID/LABEL`. Напоминание при этом отработало вживую 26.08 в 12:00 МСК («ждём 2») |
| 2026-08-27 | По итогам независимого ревью (NO-GO → исправлено). **Блокеры:** (1) лейн отчётов на 12 часов перехватывал ВЕСЬ ввод бота — после первой же пересылки переставали работать «ок»/«отмена» по брифу, `/day` и создание задач, а текст нигде не сохранялся; вдобавок `normalize_report_text` возвращала исходный текст для любого сообщения с 🔵, обесценивая строгий детектор. Теперь команды разбираются раньше лейнов, в лейне перехватывается только похожее на отчёт, секция засчитывается лишь отдельной строкой, лейн сокращён до 3 ч и имеет словесный выход; (2) `complete` считался по всем накопленным ключам, включая проекты, закрывшиеся в Redmine за неделю, — неделя закрывалась досрочно и отчёт мог собраться пустым; сдавшие считаются среди ожидаемых; (3) после сборки «без опоздавших» цикл оставался открытым навсегда и блоки прошлой недели добирали новый цикл до полноты — добавлена ротация цикла старше 6 дней. **Минорное:** присланное сохраняется дословно в `_raw/` до разбора, прежняя версия блока — в `_prev/`; предупреждения при расхождении имени по ссылке и при досылке без раздела; `find_prev_report` по времени файла (в имени нет года — в январе декабрьский отчёт считался свежее); даты по МСК; напоминание в окне 12:00–17:00 с отметкой об отправке; heartbeat после ответа Telegram. Тесты: +6, все три блокера покрыты мутационно (мутация «терять раздел Выполнено» раньше проходила незамеченной во всех 12 тестах). Итого 117 |

## Исправления стилей (2026-03-31)

- **`.filter-select`**: фон заменён с `rgba(255,255,255,0.06)` на `#1a1a2e`, добавлены `appearance: none` и `.filter-select option { background: #1a1a2e }` — для корректного тёмного оформления в разных браузерах
- **`.table-scroll`**: `max-height` изменён с `380px` на `calc(100vh - 320px)` — таблица задач теперь растягивается на весь экран
- **Блок «Высвобождение %»**: hover-тултип заменён на click-аккордеон (`.gauge-detail`); карточка «Комитет и РЦТ» растянута на всю ширину (`grid-column: 1 / -1`); при выборе фильтра `ownerFilter` нужная карточка раскрывается автоматически
- **Панель деталей вынесена за грид**: единый `#gauge-detail-panel` рендерится ниже `.gauges-grid`; при загрузке и «Все» открывается «Комитет и РЦТ»; клики блокируются при активном фильтре куратора
- **Двухколоночный layout**: `.gauges-layout` делит секцию на сетку карточек (слева) и постоянно видимую панель деталей (справа, `position: sticky`); карточки — в один столбик (`flex-direction: column`), все одинаковой ширины
- **Фикс наложения панели**: удалён `overflow: hidden` у `.section-card`; `.gauges-layout` использует `minmax(0, 1fr) minmax(0, 1fr)` и `box-sizing: border-box`; `.gauge-detail-row` — `white-space: nowrap` с `flex-shrink` на спанах
