---
paths:
  - "data.json"
  - "extract_data.py"
  - "js/**"
  - "index.html"
---

# Структура data.json

_Вынесено из `CLAUDE.md` 10.09.2026._

## Структура data.json

```
{
  updated_at  — дата/время генерации
  config      — год / redmine_base / hours_per_unit (из config.py, читает index.html)
  summary     — сводная статистика
  projects    — список проектов (из трекера "Паспорт проекта")
  all_tasks   — все задачи всех уровней (итеративный обход L1–Ln)
  curators    — данные по кураторам (штатка + высвобождение)
}
```

Поля проекта `projects[]` (дополнительные):
```
url            — ссылка на проект в Redmine (из xlsx высвобождения)
internal_hours — внутреннее высвобождение, часы (из xlsx)
external_hours — внешнее высвобождение, часы (из xlsx)
total_units    — высвобождение всего, шт.ед. (из xlsx)
closed_at      — дата закрытия (колонка «Закрыта» из Redmine)
defense_at     — дата защиты (колонка «Дата и время защиты» из Redmine)
current_status — текст статуса из еженедельного md-отчёта (process_report.py)
```

⚠️ **Форма `data.json` зависит от того, кто писал последним.** Автопрогон (без md) оставляет
`current_status` = `null` у всех проектов и нет `summary.report_updated_at`; после еженедельного
`deploy.sh` с md это строки. Тест, читающий живой `data.json`, сверять и со снимком после
`process_report` (напр. `git show a3e6cce:data.json`) — иначе он зелёный до пятницы.

## Логика сущностей

- **Проект** (`owner` = Ответственный, `manager` = Назначена)
- **Этап/Подзадача** (`executor` = Назначена)
- **Срочность задач**: `overdue` / `today` / `urgent` (≤5д) / `soon` (≤14д) / `ok`
- **Закрытые статусы**: `Закрыта`, `Закрыто`, `Выполнено`, `Выполнена`, `Завершена` — все приравниваются к закрытым; статус `Выполнено` исключается из счётчиков просрочки наравне с `Закрыта`
- **`computeTaskKPIs`**: использует `urgency` + `allTasks2026` (те же данные, что вкладка Задачи) для синхронизации KPI между вкладками; overdue подсчитывается через `t.urgency === 'overdue'`, а не через сравнение дат

## Экранирование строк в разметке

Любая строка из `data.json` попадает в `innerHTML` только через `escapeHTML` (`js/helpers.js`;
экранирует и кавычки — годится для атрибутов). Поля пишут люди в Redmine, выгрузку присылает
и коллега через Telegram-бота, `current_status` собирается из отчётов коллег — доверенного
источника нет. Экранирование терялось дважды: R5 (16.06.2026, `52018cc`) и при разрезе на
`js/render/*` (исправлено 25.09.2026).

Сторож `tests/test_js_escape.py` разбирает шаблонные строки в `js/` и краснеет на `${d.поле}`
без `escapeHTML`. Не видит склейку через `+`, переименование в промежуточную переменную и
`d['поле']` — так в разметку не писать. Новое строковое поле `data.json` валит тест, пока его
не внесут в `DATA_FIELDS`.
